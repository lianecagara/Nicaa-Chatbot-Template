/**
 * DO NOT EDIT THIS FILE DIRECTLY.
 * This file is generated following the conversion of 
 * @see [./node_modules/glob-parent/index.js]{@link ./node_modules/glob-parent/index.js}
 * 
 **/
import path  from "path";
import isglob  from "../is-glob/index.mjs";
import pathDirname  from "../path-dirname/index.mjs";
import _toesmTemp29  from "os";
'use strict';





var isWin32  = _toesmTemp29.platform() === 'win32';


export default function globParent(str) {
	// flip windows path separators
	if (isWin32 && str.indexOf('/') < 0) str = str.split('\\').join('/');

	// special case for strings ending in enclosure containing path separator
	if (/[\{\[].*[\/]*.*[\}\]]$/.test(str)) str += '/';

	// preserves full path in case of trailing path separator
	str += 'a';

	// remove path parts that are globby
	do {str = pathDirname.posix(str)}
	while (isglob(str) || /(^|[^\\])([\{\[]|\([^\)]+$)/.test(str));

	// remove escape chars and return result
	return str.replace(/\\([\*\?\|\[\]\(\)\{\}])/g, '$1');
};
